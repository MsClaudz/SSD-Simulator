import DictBuilder
trace_file = 'sample_trace.txt'
sectors_per_block = 8

test_dict = DictBuilder.build_dict(trace_file, sectors_per_block)
print(test_dict)

# given update frequency ratio, compute number of partitions

# find max and min values

# divide max by half until you get to min value

# return number 

# given number of partitions, compute update frequency ratio

